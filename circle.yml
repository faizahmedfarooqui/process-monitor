machine:
    timezone:
        Asia/Kolkata
    php:
        version: 7.0.4

# checkout:
#     post:
#         - git submodule sync
#         - git submodule update --init

# dependencies:
#     pre:
#         - composer install

test:
    override:
        - php ./index.php

    post:
        - mkdir -p $CIRLCE_ARTIFACTS/json_output
        - mv solo/target/*.json $CIRLCE_ARTIFACTS/json_output

